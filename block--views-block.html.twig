<div{{ attributes.addclass('badges') }}>
    <div class="summary">
  {{ title_prefix }}
  {% if label %}
    <h3{{ title_attributes.addclass('title') }}>{{ label }}</h3>
  {% endif %}
  {{ title_suffix }}
  {% block content %}
    {{ content }}
  {% endblock %}
</div>
</div>